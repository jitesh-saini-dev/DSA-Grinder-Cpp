#include <bits/stdc++.h>
using namespace std;
int main()
{
    stack<int> s;
    int n = 1234;
    while (n)
    {
        int temp = n % 10;
    }

    while (!s.empty())
    {
        cout << s.top() << " ";
        s.pop();
    }
}